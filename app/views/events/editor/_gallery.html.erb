<div class="gallery">
  <div class='gallery-header'>
    <a href="#" class="prev"><</a>
    <div class='category-name'><%= categories.first.name.upcase %></div>
    <a href="#" class="next">></a>
  </div>
  <div class='slide'>
    <% categories.each do |category| %>
      <div class="themes" data-category-name="<%= category.name.upcase %>">
        <% category.events.themes.each do |theme| %>
          <div class='gallery-theme' data-theme="<%= theme.id %>" data-category="<%= category.id %>" style="background: url('<%= theme.thumbnail_url %>') no-repeat; background-size: cover;"></div>
        <% end %>
      </div>
    <% end %>
    <!-- COMING SOON -->
    <div class="themes" data-category-name="CELEBRATION">
      <div>Coming Soon..</div>
    </div>
    <div class="themes" data-category-name="MEETUPS">
      <div>Coming Soon..</div>
    </div>
    <div class="themes" data-category-name="WHATEVER">
      <div>Coming Soon..</div>
    </div>
    <!--  -->
  </div>
</div>