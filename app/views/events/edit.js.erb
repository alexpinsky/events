$(".edit-event").html('<%= j render partial: "form_and_preview", locals: { event: @event } %>');
addthisevent.refresh();
$(document).foundation();
editor = new Editor({editor: $("#editor"), preview: $("#preview")})
editor.init()