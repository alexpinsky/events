<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta property="og:title" content="MyEvent" />
    <meta property="og:image" content="<%= example_events_image_url %>" />
    <title><%= content_for?(:title) ? yield(:title) : "MyEvent" %></title>
    <%= stylesheet_link_tag  "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <!-- top bar -->
    <nav class="top-bar" data-topbar data-options="is_hover: false">
      <ul class="title-area">
        <li class="name">
          <h1><a href="#">MyEvent</a></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
      </ul>
      <section class="top-bar-section">
        <ul class="right">
        <% if current_user %>
        <!-- Right Nav Section -->
          <li class="has-dropdown">
            <a href="#"><%= current_user.email %></a>
            <ul class="dropdown">
              <li><%= link_to 'Sign out', destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
        <li class="has-form">
          <a href="#" class="button" data-reveal-id="singInModal">Sign In</a>
          <!-- Sign In modal -->
          <div id="singInModal" class="reveal-modal small" data-reveal>
            <%= render partial: 'devise/shared/sign_in' %>
            <a class="close-reveal-modal">&#215;</a>
          </div>
        </li>
        <% end %>
        </ul>
      </section>
    </nav>
    <!-- main container -->
    <div class="main-container">
      <div class="row">
        <div class="small-12 small-centered columns">
          <div class="flashes small-4 small-offset-10 columns">
            <%= render partial: "shared/flashes" if flash.present? %>
          </div>
          <%= yield %>
        </div>
      </div>
    </div>
    <div class="footer">
      <div class="row">
        <div class="large-8 large-offset-1 columns">
          <div class="logo">MyEvent</div>
          <div class="links">
            <ul>
              <li><a href="#">About</a></li>
              <li><a href="#">Contact</a></li>
            </ul>
          </div>
        </div>
      </div>
    </div>
    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= yield(:javascript) %>
    <script type="text/javascript" src="http://js.addthisevent.com/atemay.js"></script>
  </body>
</html>