var eventDiv = $(".event");
eventDiv.data('json', '<%= event_json.html_safe %>');
eventDiv.html(
  '<%= j render partial: "events/editor", locals: { event: event, theme: theme, categories: categories } %>'
);

Editor.start();