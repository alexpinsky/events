<% slideshow = if images.size > 1 %>
  <% true %>
<% else %>
  <% false %>
<% end %>
<div class='images' data-slideshow="<%= slideshow %>">
  <% images.each do |pic| %>
    <img id="image-<%= pic.order %>" src="<%= pic.image_url %>">
  <% end %>
</div>